# Telegram-бот для расчёта стоимости доставки мебели

Бот автоматизирует расчёт стоимости доставки и подъёма мебели на основе прайс-листа.

## 🚀 Развертывание на Amvera

Этот проект настроен для автоматического развертывания на [Amvera](https://amvera.ru).

### Файлы проекта:
- `bot.py` - основной файл бота
- `app.py` - точка входа для Amvera
- `amvera.yml` - конфигурация Amvera
- `requirements.txt` - зависимости Python
- `prices.json` - прайс-лист с ценами

### Автоматическое развертывание:

1. Подключите этот репозиторий в панели Amvera
2. Amvera автоматически:
   - Установит зависимости из `requirements.txt`
   - Запустит бота через `app.py`
   - Бот начнет работать

## 📋 Зависимости

- `python-telegram-bot==20.7` - библиотека для работы с Telegram Bot API
- `python-dotenv==1.0.0` - загрузка переменных окружения
- `httpx==0.25.2` - HTTP клиент для загрузки данных из Google Sheets

## 🔧 Локальная установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram

3. Токен уже указан в `bot.py` (строка 3112)

4. Запустите бота:
```bash
python bot.py
```

## 📊 Структура прайса

Все цены хранятся в файле `prices.json`. Бот также может загружать актуальные цены из Google Sheets.

## 🔐 Админ-панель

Бот имеет админ-панель для:
- Просмотра прайс-листа
- Обновления цен из Google Sheets

Доступ только для администратора (ID указан в `bot.py`).

## 📝 Лицензия

Проект для внутреннего использования.
